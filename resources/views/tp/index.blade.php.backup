@extends('layouts.ki-admin')

@section('title', 'Mes Travaux Pratiques - Community Management')
@section('page-title', 'Travaux Pratiques - Community Management')

@section('content')
<style>
    :root {
        --primary-blue: #4fc3f7;
        --primary-blue-dark: #29b6f6;
        --secondary-orange: #ff9800;
        --secondary-orange-dark: #fb8c00;
        --dark-blue: #1e3c72;
        --success-green: #10b981;
        --danger-red: #ef4444;
    }

    .page-header {
        background: linear-gradient(135deg, var(--primary-blue) 0%, var(--primary-blue-dark) 100%);
        padding: 2rem;
        border-radius: 20px;
        color: white;
        margin-bottom: 2rem;
        box-shadow: 0 10px 30px rgba(79, 195, 247, 0.3);
    }

    .page-header h1 {
        font-size: 2.5rem;
        font-weight: 800;
        margin-bottom: 0.5rem;
    }

    .page-header p {
        font-size: 1.1rem;
        opacity: 0.95;
    }

    /* Section Headers */
    .section-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 1.5rem;
        padding-bottom: 1rem;
        border-bottom: 3px solid #e5e7eb;
    }

    .section-title {
        font-size: 1.8rem;
        font-weight: 800;
        color: #1e293b;
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }

    .section-title i {
        color: var(--primary-blue);
    }

    /* Statistics Cards */
    .stat-card {
        border: none;
        border-radius: 20px;
        overflow: hidden;
        transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        position: relative;
        background: white;
        box-shadow: 0 5px 20px rgba(0,0,0,0.08);
        height: 100%;
    }
    
    .stat-card:hover {
        transform: translateY(-10px) scale(1.02);
        box-shadow: 0 20px 40px rgba(0,0,0,0.15);
    }
    
    .stat-card.blue {
        background: linear-gradient(135deg, var(--primary-blue) 0%, var(--primary-blue-dark) 100%);
        color: white;
    }
    
    .stat-card.orange {
        background: linear-gradient(135deg, var(--secondary-orange) 0%, var(--secondary-orange-dark) 100%);
        color: white;
    }
    
    .stat-card.green {
        background: linear-gradient(135deg, #059669 0%, var(--success-green) 100%);
        color: white;
    }
    
    .stat-card.purple {
        background: linear-gradient(135deg, #9c27b0 0%, #7b1fa2 100%);
        color: white;
    }
    
    .stat-number {
        font-size: 3.5rem;
        font-weight: 900;
        line-height: 1;
        text-shadow: 2px 2px 8px rgba(0,0,0,0.2);
        margin: 1rem 0;
    }
    
    .stat-label {
        font-size: 0.95rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 1.5px;
        opacity: 0.95;
    }
    
    .stat-icon {
        font-size: 3rem;
        opacity: 0.2;
        position: absolute;
        right: 20px;
        top: 20px;
    }

    /* Progress Bar */
    .progress-card {
        border: none;
        border-radius: 20px;
        background: white;
        box-shadow: 0 5px 20px rgba(0,0,0,0.08);
        overflow: hidden;
        padding: 2rem;
    }
    
    .progress-bar-custom {
        height: 35px;
        border-radius: 20px;
        background: linear-gradient(90deg, var(--primary-blue) 0%, var(--secondary-orange) 100%);
        background-size: 200% 100%;
        animation: progressGlow 3s ease-in-out infinite;
        box-shadow: 0 4px 15px rgba(79, 195, 247, 0.4);
        position: relative;
        overflow: hidden;
    }

    .progress-bar-custom::after {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
        animation: shimmer 2s infinite;
    }
    
    @keyframes progressGlow {
        0%, 100% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
    }

    @keyframes shimmer {
        0% { left: -100%; }
        100% { left: 100%; }
    }
    
    /* Travaux en Attente Cards */
    .tp-pending-card {
        border: none;
        border-radius: 15px;
        background: white;
        box-shadow: 0 3px 15px rgba(0,0,0,0.08);
        overflow: hidden;
        transition: all 0.3s ease;
        border-left: 5px solid var(--secondary-orange);
    }

    .tp-pending-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 25px rgba(0,0,0,0.12);
    }

    .tp-pending-header {
        background: linear-gradient(135deg, rgba(255, 152, 0, 0.1) 0%, rgba(251, 140, 0, 0.1) 100%);
        padding: 1rem 1.5rem;
        border-bottom: 2px solid rgba(255, 152, 0, 0.2);
    }

    .tp-pending-body {
        padding: 1.5rem;
    }

    .tp-title {
        font-weight: 700;
        color: #1e293b;
        font-size: 1.1rem;
        margin-bottom: 0.5rem;
    }
    
    .tp-description {
        color: #64748b;
        font-size: 0.9rem;
        line-height: 1.6;
    }

    .tp-meta {
        display: flex;
        align-items: center;
        gap: 1.5rem;
        margin-top: 1rem;
        padding-top: 1rem;
        border-top: 1px solid #e5e7eb;
    }

    .tp-meta-item {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-size: 0.9rem;
        color: #64748b;
    }
    
    .badge-category {
        padding: 0.5rem 1rem;
        border-radius: 20px;
        font-weight: 700;
        font-size: 0.85rem;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }
    
    .badge-digital {
        background: linear-gradient(135deg, var(--primary-blue) 0%, var(--primary-blue-dark) 100%);
        color: white;
    }
    
    .badge-print {
        background: linear-gradient(135deg, var(--secondary-orange) 0%, var(--secondary-orange-dark) 100%);
        color: white;
    }
    
    .badge-status {
        padding: 0.4rem 0.9rem;
        border-radius: 20px;
        font-weight: 700;
        font-size: 0.8rem;
    }
    
    .badge-pending {
        background: linear-gradient(135deg, var(--secondary-orange) 0%, var(--secondary-orange-dark) 100%);
        color: white;
    }
    
    .badge-validated {
        background: linear-gradient(135deg, #059669 0%, var(--success-green) 100%);
        color: white;
    }

    .badge-rejected {
        background: linear-gradient(135deg, #dc2626 0%, var(--danger-red) 100%);
        color: white;
    }

    /* Action Buttons */
    .btn-action {
        border: none;
        padding: 0.75rem 1.5rem;
        border-radius: 12px;
        font-weight: 700;
        transition: all 0.3s ease;
        text-transform: uppercase;
        font-size: 0.9rem;
        letter-spacing: 0.5px;
    }
    
    .btn-action.btn-primary-custom {
        background: linear-gradient(135deg, var(--primary-blue) 0%, var(--primary-blue-dark) 100%);
        color: white;
        box-shadow: 0 4px 15px rgba(79, 195, 247, 0.3);
    }
    
    .btn-action.btn-primary-custom:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 25px rgba(79, 195, 247, 0.4);
    }

    .btn-action.btn-orange-custom {
        background: linear-gradient(135deg, var(--secondary-orange) 0%, var(--secondary-orange-dark) 100%);
        color: white;
        box-shadow: 0 4px 15px rgba(255, 152, 0, 0.3);
    }
    
    .btn-action.btn-orange-custom:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 25px rgba(255, 152, 0, 0.4);
    }

    .btn-action.btn-success-custom {
        background: linear-gradient(135deg, #059669 0%, var(--success-green) 100%);
        color: white;
        box-shadow: 0 4px 15px rgba(16, 185, 129, 0.3);
    }
    
    .btn-action.btn-success-custom:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 25px rgba(16, 185, 129, 0.4);
    }

    /* Add Project Buttons */
    .btn-add-project {
        position: relative;
        overflow: hidden;
        border: none;
        padding: 2rem;
        border-radius: 20px;
        font-weight: 800;
        transition: all 0.4s ease;
        text-transform: uppercase;
        font-size: 1.1rem;
        letter-spacing: 1px;
        color: white;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 1rem;
        box-shadow: 0 8px 30px rgba(0,0,0,0.15);
    }

    .btn-add-project i {
        font-size: 3rem;
        animation: pulse 2s infinite;
    }

    .btn-add-project:hover {
        transform: translateY(-10px) scale(1.05);
        box-shadow: 0 15px 40px rgba(0,0,0,0.25);
    }

    .btn-add-digital {
        background: linear-gradient(135deg, var(--primary-blue) 0%, var(--primary-blue-dark) 100%);
    }

    .btn-add-print {
        background: linear-gradient(135deg, var(--secondary-orange) 0%, var(--secondary-orange-dark) 100%);
    }

    @keyframes pulse {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.1); }
    }
    
    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(30px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
    
    .fade-in-up {
        animation: fadeInUp 0.6s ease-out;
    }

    /* Empty State */
    .empty-state {
        text-align: center;
        padding: 3rem 2rem;
        color: #64748b;
    }

    .empty-state i {
        font-size: 4rem;
        color: #cbd5e1;
        margin-bottom: 1rem;
    }

    .empty-state h4 {
        color: #475569;
        margin-bottom: 0.5rem;
    }
</style>

<!-- Page Header -->
<div class="page-header mb-4">
    <h1>
        <i class="fas fa-tasks me-3"></i>
        Mes Travaux Pratiques
    </h1>
    <p class="mb-0">Gérez vos projets digitaux et print en Community Management</p>
</div>

<div class="row">
    <div class="col-12">
        <!-- Section 1: Statistiques -->
        <div class="section-header">
            <h2 class="section-title">
                <i class="fas fa-chart-pie"></i>
                Statistiques
            </h2>
        </div>

        <!-- Cartes de Statistiques -->
        <div class="row mb-5">
            <!-- Total TP -->
            <div class="col-lg-3 col-md-6 mb-4 fade-in-up" style="animation-delay: 0.1s;">
                <div class="stat-card blue">
                    <i class="fas fa-tasks stat-icon"></i>
                    <div class="card-body p-4 text-center">
                        <div class="stat-label mb-2">Total TP</div>
                        <div class="stat-number">{{ isset($statistiques) ? $statistiques['tp_realises'] : 0 }}</div>
                        <p class="mb-0 mt-2" style="font-size: 0.9rem; opacity: 0.9;">
                            Projets soumis
                        </p>
                    </div>
                </div>
            </div>
            
            <!-- TP En Attente -->
            <div class="col-lg-3 col-md-6 mb-4 fade-in-up" style="animation-delay: 0.2s;">
                <div class="stat-card orange">
                    <i class="fas fa-clock stat-icon"></i>
                    <div class="card-body p-4 text-center">
                        <div class="stat-label mb-2">En Attente</div>
                        <div class="stat-number">
                            @if(isset($validationStats) && is_array($validationStats))
                                {{ $validationStats['tp_en_validation'] ?? 0 }}
                            @else
                                0
                            @endif
                        </div>
                        <p class="mb-0 mt-2" style="font-size: 0.9rem; opacity: 0.9;">
                            En validation
                        </p>
                    </div>
                </div>
            </div>
            
            <!-- TP Validés -->
            <div class="col-lg-3 col-md-6 mb-4 fade-in-up" style="animation-delay: 0.3s;">
                <div class="stat-card green">
                    <i class="fas fa-check-circle stat-icon"></i>
                    <div class="card-body p-4 text-center">
                        <div class="stat-label mb-2">Validés</div>
                        <div class="stat-number">
                            @if(isset($validationStats) && is_array($validationStats))
                                {{ $validationStats['tp_valides'] ?? 0 }}
                            @else
                                0
                            @endif
                        </div>
                        <p class="mb-0 mt-2" style="font-size: 0.9rem; opacity: 0.9;">
                            Projets approuvés
                        </p>
                    </div>
                </div>
            </div>
            
            <!-- Taux de Réussite -->
            <div class="col-lg-3 col-md-6 mb-4 fade-in-up" style="animation-delay: 0.4s;">
                <div class="stat-card purple">
                    <i class="fas fa-trophy stat-icon"></i>
                    <div class="card-body p-4 text-center">
                        <div class="stat-label mb-2">Taux Réussite</div>
                        <div class="stat-number">
                            @php
                                $total = isset($statistiques) ? $statistiques['tp_realises'] : 0;
                                $valides = isset($validationStats) && is_array($validationStats) ? ($validationStats['tp_valides'] ?? 0) : 0;
                                $taux = $total > 0 ? round(($valides / $total) * 100) : 0;
                            @endphp
                            {{ $taux }}%
                        </div>
                        <p class="mb-0 mt-2" style="font-size: 0.9rem; opacity: 0.9;">
                            Performance globale
                        </p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Section 2: Niveau de Progression des TP -->
        <div class="section-header mt-5">
            <h2 class="section-title">
                <i class="fas fa-chart-line"></i>
                Niveau de Progression des TP
            </h2>
        </div>

        @if(isset($statistiques))
        <div class="row mb-5 fade-in-up">
            <div class="col-12">
                <div class="progress-card">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <h5 class="mb-0 fw-bold" style="color: #1e293b;">
                            <i class="fas fa-graduation-cap me-2" style="color: var(--primary-blue);"></i>
                            Progression Formation Community Management
                        </h5>
                        <span class="badge" style="background: linear-gradient(135deg, var(--primary-blue) 0%, var(--secondary-orange) 100%); color: white; padding: 0.7rem 1.5rem; border-radius: 25px; font-size: 1.2rem; font-weight: 800;">
                            {{ $statistiques['progression_pourcentage'] }}%
                        </span>
                    </div>
                    <div class="progress" style="height: 35px; border-radius: 20px; background: #e5e7eb;">
                        <div class="progress-bar-custom" role="progressbar"
                             style="width: {{ $statistiques['progression_pourcentage'] }}%"
                             aria-valuenow="{{ $statistiques['progression_pourcentage'] }}"
                             aria-valuemin="0" aria-valuemax="100">
                            <span class="fw-bold px-3" style="font-size: 1rem;">{{ $statistiques['progression_pourcentage'] }}%</span>
                        </div>
                    </div>
                    <div class="row mt-4">
                        <div class="col-md-4 text-center">
                            <div class="p-3">
                                <i class="fas fa-check-circle text-success" style="font-size: 2rem;"></i>
                                <h4 class="mt-2 mb-1" style="color: #1e293b; font-weight: 700;">{{ $statistiques['tp_realises'] }}</h4>
                                <p class="text-muted mb-0">TP Réalisés</p>
                            </div>
                        </div>
                        <div class="col-md-4 text-center">
                            <div class="p-3">
                                <i class="fas fa-list-check" style="font-size: 2rem; color: var(--primary-blue);"></i>
                                <h4 class="mt-2 mb-1" style="color: #1e293b; font-weight: 700;">{{ $statistiques['tp_total'] }}</h4>
                                <p class="text-muted mb-0">TP Requis</p>
                            </div>
                        </div>
                        <div class="col-md-4 text-center">
                            <div class="p-3">
                                <i class="fas fa-hourglass-half" style="font-size: 2rem; color: var(--secondary-orange);"></i>
                                <h4 class="mt-2 mb-1" style="color: #1e293b; font-weight: 700;">{{ $statistiques['tp_a_faire'] }}</h4>
                                <p class="text-muted mb-0">TP Restants</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        @endif

        <!-- Section 3: Travaux en Attente -->
        <div class="section-header mt-5">
            <h2 class="section-title">
                <i class="fas fa-clock"></i>
                Travaux en Attente de Validation
            </h2>
            <span class="badge badge-pending" style="font-size: 1rem; padding: 0.6rem 1.2rem;">
                @if(isset($validationStats) && is_array($validationStats))
                    {{ $validationStats['tp_en_validation'] ?? 0 }}
                @else
                    0
                @endif
                en attente
            </span>
        </div>

        <div class="row mb-5">
            @if(isset($tps) && count($tps) > 0)
                @php
                    $tpsPending = collect($tps)->where('status', 'pending');
                @endphp
                
                @if($tpsPending->count() > 0)
                    @foreach($tpsPending->take(6) as $tp)
                    <div class="col-lg-4 col-md-6 mb-4">
                        <div class="tp-pending-card">
                            <div class="tp-pending-header">
                                <div class="d-flex justify-content-between align-items-center">
                                    <span class="badge badge-{{ $tp->type ?? 'digital' }}">
                                        <i class="fas fa-{{ ($tp->type ?? 'digital') == 'digital' ? 'laptop' : 'print' }} me-1"></i>
                                        {{ ucfirst($tp->type ?? 'Digital') }}
                                    </span>
                                    <span class="badge badge-pending">
                                        <i class="fas fa-clock me-1"></i>
                                        En attente
                                    </span>
                                </div>
                            </div>
                            <div class="tp-pending-body">
                                <h5 class="tp-title">{{ $tp->title ?? 'Sans titre' }}</h5>
                                <p class="tp-description">{{ Str::limit($tp->description ?? 'Aucune description', 100) }}</p>
                                
                                <div class="tp-meta">
                                    <div class="tp-meta-item">
                                        <i class="fas fa-calendar"></i>
                                        <span>{{ \Carbon\Carbon::parse($tp->created_at)->format('d/m/Y') }}</span>
                                    </div>
                                    @if(isset($tp->files_count))
                                    <div class="tp-meta-item">
                                        <i class="fas fa-paperclip"></i>
                                        <span>{{ $tp->files_count }} fichier(s)</span>
                                    </div>
                                    @endif
                                </div>

                                <div class="mt-3">
                                    <a href="{{ route('design-graphique.tp.voir', $tp->id) }}" class="btn btn-action btn-primary-custom w-100">
                                        <i class="fas fa-eye me-2"></i>
                                        Voir Détails
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                    @endforeach
                @else
                    <div class="col-12">
                        <div class="card border-0 shadow-sm">
                            <div class="card-body">
                                <div class="empty-state">
                                    <i class="fas fa-check-circle"></i>
                                    <h4>Aucun travail en attente</h4>
                                    <p>Tous vos travaux ont été traités !</p>
                                </div>
                            </div>
                        </div>
                    </div>
                @endif
            @else
                <div class="col-12">
                    <div class="card border-0 shadow-sm">
                        <div class="card-body">
                            <div class="empty-state">
                                <i class="fas fa-folder-open"></i>
                                <h4>Aucun travail soumis</h4>
                                <p>Commencez par ajouter votre premier projet !</p>
                            </div>
                        </div>
                    </div>
                </div>
            @endif
        </div>

        <!-- Section 4: Ajouter un Projet -->
        <div class="section-header mt-5">
            <h2 class="section-title">
                <i class="fas fa-plus-circle"></i>
                Ajouter un Nouveau Projet
            </h2>
        </div>

        <div class="row mb-5">
            <!-- Ajouter Projet Digital -->
            <div class="col-lg-6 mb-4">
                <a href="{{ route('design-graphique.tp.ajouter') }}?type=digital" class="btn btn-add-project btn-add-digital w-100 text-decoration-none">
                    <i class="fas fa-laptop"></i>
                    <span>Ajouter un Projet Digital</span>
                    <small style="font-size: 0.85rem; font-weight: 600; text-transform: none; opacity: 0.9;">
                        Réseaux sociaux, campagnes digitales, content marketing
                    </small>
                </a>
            </div>

            <!-- Ajouter Projet Print -->
            <div class="col-lg-6 mb-4">
                <a href="{{ route('design-graphique.tp.ajouter') }}?type=print" class="btn btn-add-project btn-add-print w-100 text-decoration-none">
                    <i class="fas fa-print"></i>
                    <span>Ajouter un Projet Print</span>
                    <small style="font-size: 0.85rem; font-weight: 600; text-transform: none; opacity: 0.9;">
                        Flyers, affiches, brochures, supports imprimés
                    </small>
                </a>
            </div>
        </div>

        <!-- Section 5: Tous mes TP -->
        <div class="section-header mt-5">
            <h2 class="section-title">
                <i class="fas fa-list"></i>
                Tous mes Travaux Pratiques
            </h2>
            <a href="{{ route('design-graphique.tp.tous') }}" class="btn btn-action btn-success-custom">
                <i class="fas fa-eye me-2"></i>
            </a>
        </div>

        <div class="row mb-5">
            @if(isset($tps) && count($tps) > 0)
                @foreach($tps->take(6) as $tp)
                <div class="col-lg-4 col-md-6 mb-4">
                    <div class="tp-pending-card">
                        <div class="tp-pending-header">
                            <div class="d-flex justify-content-between align-items-center">
                                <span class="badge badge-{{ $tp->type ?? 'digital' }}">
                                    <i class="fas fa-{{ ($tp->type ?? 'digital') == 'digital' ? 'laptop' : 'print' }} me-1"></i>
                                    {{ ucfirst($tp->type ?? 'Digital') }}
                                </span>
                                @if(isset($tp->status))
                                    <span class="badge badge-{{ $tp->status }}">
                                        <i class="fas fa-{{ $tp->status == 'validated' ? 'check-circle' : ($tp->status == 'rejected' ? 'times-circle' : 'clock') }} me-1"></i>
                                        {{ $tp->status == 'validated' ? 'Validé' : ($tp->status == 'rejected' ? 'Rejeté' : 'En attente') }}
                                    </span>
                                @endif
                            </div>
                        </div>
                        <div class="tp-pending-body">
                            <h5 class="tp-title">{{ $tp->title ?? 'Sans titre' }}</h5>
                            <p class="tp-description">{{ Str::limit($tp->description ?? 'Aucune description', 100) }}</p>
                            
                            <div class="tp-meta">
                                <div class="tp-meta-item">
                                    <i class="fas fa-calendar"></i>
                                    <span>{{ \Carbon\Carbon::parse($tp->created_at)->format('d/m/Y') }}</span>
                                </div>
                                @if(isset($tp->files_count))
                                <div class="tp-meta-item">
                                    <i class="fas fa-paperclip"></i>
                                    <span>{{ $tp->files_count }} fichier(s)</span>
                                </div>
                                @endif
                            </div>

                            <div class="mt-3">
                                <a href="{{ route('design-graphique.tp.voir', $tp->id) }}" class="btn btn-action btn-primary-custom w-100">
                                    <i class="fas fa-eye me-2"></i>
                                    Voir Détails
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
                @endforeach
            @else
                <div class="col-12">
                    <div class="card border-0 shadow-sm">
                        <div class="card-body">
                            <div class="empty-state">
                                <i class="fas fa-folder-open"></i>
                                <h4>Aucun travail pratique</h4>
                                <p>Commencez par ajouter votre premier projet digital ou print !</p>
                                <div class="mt-4">
                                    <a href="{{ route('design-graphique.tp.ajouter') }}?type=digital" class="btn btn-action btn-primary-custom me-2">
                                        <i class="fas fa-plus me-2"></i>
                                        Ajouter un TP
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            @endif
        </div>

        <!-- Ancien contenu supprimé -->
        <div class="tp-list-card mb-5 fade-in-up" style="animation-delay: 0.6s;">
            <div class="tp-list-header">
                <h4 class="mb-0 fw-bold">
                    <i class="fas fa-tasks me-3"></i>
                    Travaux Pratiques à Réaliser
                </h4>
                <p class="mb-0 mt-2 opacity-75">Complétez vos TP pour progresser dans votre formation</p>
            </div>
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-hover mb-0">
                        <thead style="background: #f8fafc; border-bottom: 2px solid #e2e8f0;">
                            <tr>
                                <th class="py-3 px-4 fw-bold text-uppercase" style="font-size: 0.85rem; letter-spacing: 0.5px; color: #64748b;">TP</th>
                                <th class="py-3 px-4 fw-bold text-uppercase" style="font-size: 0.85rem; letter-spacing: 0.5px; color: #64748b;">Catégorie</th>
                                <th class="py-3 px-4 fw-bold text-uppercase" style="font-size: 0.85rem; letter-spacing: 0.5px; color: #64748b;">Échéance</th>
                                <th class="py-3 px-4 fw-bold text-uppercase" style="font-size: 0.85rem; letter-spacing: 0.5px; color: #64748b;">Priorité</th>
                                <th class="py-3 px-4 fw-bold text-uppercase" style="font-size: 0.85rem; letter-spacing: 0.5px; color: #64748b;">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr class="tp-item-row">
                                <td class="py-4 px-4">
                                    <div>
                                        <h6 class="tp-title mb-1">TP Photoshop - Retouche Portrait</h6>
                                        <p class="tp-description mb-0">Retouche professionnelle d'un portrait avec techniques avancées</p>
                                    </div>
                                </td>
                                <td class="py-4 px-4">
                                    <span class="badge badge-category badge-photoshop">
                                        <i class="fas fa-image me-1"></i>
                                        Photoshop
                                    </span>
                                </td>
                                <td class="py-4 px-4">
                                    <div class="d-flex align-items-center text-danger">
                                        <i class="fas fa-calendar-alt me-2"></i>
                                        <span class="fw-semibold">30 Juillet 2024</span>
                                    </div>
                                </td>
                                <td class="py-4 px-4">
                                    <span class="badge badge-priority badge-urgent">
                                        <i class="fas fa-exclamation-circle me-1"></i>
                                        Urgent
                                    </span>
                                </td>
                                <td class="py-4 px-4">
                                    <div class="d-flex gap-2">
                                        <button class="tp-action-btn btn-start" title="Commencer">
                                            <i class="fas fa-play me-1"></i>
                                            Commencer
                                        </button>
                                        <button class="tp-action-btn btn-info" title="Détails">
                                            <i class="fas fa-info-circle"></i>
                                        </button>
                                    </div>
                                </td>
                            </tr>
                            
                            <tr class="tp-item-row">
                                <td class="py-4 px-4">
                                    <div>
                                        <h6 class="tp-title mb-1">TP InDesign - Magazine Layout</h6>
                                        <p class="tp-description mb-0">Création d'une mise en page de magazine avec grilles et typographie</p>
                                    </div>
                                </td>
                                <td class="py-4 px-4">
                                    <span class="badge badge-category badge-indesign">
                                        <i class="fas fa-file-alt me-1"></i>
                                        InDesign
                                    </span>
                                </td>
                                <td class="py-4 px-4">
                                    <div class="d-flex align-items-center text-muted">
                                        <i class="fas fa-calendar-alt me-2"></i>
                                        <span class="fw-semibold">5 Août 2024</span>
                                    </div>
                                </td>
                                <td class="py-4 px-4">
                                    <span class="badge badge-priority badge-normal">
                                        <i class="fas fa-info-circle me-1"></i>
                                        Normal
                                    </span>
                                </td>
                                <td class="py-4 px-4">
                                    <div class="d-flex gap-2">
                                        <button class="tp-action-btn btn-start" title="Commencer">
                                            <i class="fas fa-play me-1"></i>
                                            Commencer
                                        </button>
                                        <button class="tp-action-btn btn-info" title="Détails">
                                            <i class="fas fa-info-circle"></i>
                                        </button>
                                    </div>
                                </td>
                            </tr>
                            
                            <tr class="tp-item-row">
                                <td class="py-4 px-4">
                                    <div>
                                        <h6 class="tp-title mb-1">TP Illustrator - Logo Design</h6>
                                        <p class="tp-description mb-0">Création d'un logo vectoriel avec techniques avancées</p>
                                    </div>
                                </td>
                                <td class="py-4 px-4">
                                    <span class="badge badge-category badge-illustrator">
                                        <i class="fas fa-vector-square me-1"></i>
                                        Illustrator
                                    </span>
                                </td>
                                <td class="py-4 px-4">
                                    <div class="d-flex align-items-center text-muted">
                                        <i class="fas fa-calendar-alt me-2"></i>
                                        <span class="fw-semibold">10 Août 2024</span>
                                    </div>
                                </td>
                                <td class="py-4 px-4">
                                    <span class="badge badge-priority badge-normal">
                                        <i class="fas fa-info-circle me-1"></i>
                                        Normal
                                    </span>
                                </td>
                                <td class="py-4 px-4">
                                    <div class="d-flex gap-2">
                                        <button class="tp-action-btn btn-start" title="Commencer">
                                            <i class="fas fa-play me-1"></i>
                                            Commencer
                                        </button>
                                        <button class="tp-action-btn btn-info" title="Détails">
                                            <i class="fas fa-info-circle"></i>
                                        </button>
                                    </div>
                                </td>
                            </tr>
                            
                            <tr class="tp-item-row">
                                <td class="py-4 px-4">
                                    <div>
                                        <h6 class="tp-title mb-1">TP Strategy Business - Plan d'affaires</h6>
                                        <p class="tp-description mb-0">Création d'un plan d'affaires pour une entreprise de création graphique</p>
                                    </div>
                                </td>
                                <td class="py-4 px-4">
                                    <span class="badge badge-category badge-masterclass">
                                        <i class="fas fa-crown me-1"></i>
                                        Master Class
                                    </span>
                                </td>
                                <td class="py-4 px-4">
                                    <div class="d-flex align-items-center text-muted">
                                        <i class="fas fa-calendar-alt me-2"></i>
                                        <span class="fw-semibold">15 Août 2024</span>
                                    </div>
                                </td>
                                <td class="py-4 px-4">
                                    <span class="badge badge-priority badge-normal">
                                        <i class="fas fa-info-circle me-1"></i>
                                        Normal
                                    </span>
                                </td>
                                <td class="py-4 px-4">
                                    <div class="d-flex gap-2">
                                        <button class="tp-action-btn btn-start" title="Commencer">
                                            <i class="fas fa-play me-1"></i>
                                            Commencer
                                        </button>
                                        <button class="tp-action-btn btn-info" title="Détails">
                                            <i class="fas fa-info-circle"></i>
                                        </button>
                                    </div>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <!-- Boutons Ajouter Projets -->
        <div class="row mb-4">
            <div class="col-12">
                <div class="row justify-content-center">
                    <!-- Bouton Projet Digital -->
                    <div class="col-md-5 col-12 mb-3 text-center">
                        <div class="add-tp-container position-relative">
                            <a href="{{ route('design-graphique.tp.ajouter') }}?type=digital" class="btn btn-add-tp btn-digital btn-lg px-4 py-3 position-relative overflow-hidden text-decoration-none w-100">
                                <span class="btn-content position-relative z-index-2">
                                    <i class="fas fa-laptop me-2 pulse-icon"></i>
                                    Ajouter un Projet Digital
                                </span>
                                <div class="wave-effect"></div>
                                <div class="wave-effect wave-2"></div>
                                <div class="wave-effect wave-3"></div>
                            </a>
                        </div>
                    </div>

                    <!-- Bouton Projet Print -->
                    <div class="col-md-5 col-12 mb-3 text-center">
                        <div class="add-tp-container position-relative">
                            <a href="{{ route('design-graphique.tp.ajouter') }}?type=print" class="btn btn-add-tp btn-print btn-lg px-4 py-3 position-relative overflow-hidden text-decoration-none w-100">
                                <span class="btn-content position-relative z-index-2">
                                    <i class="fas fa-print me-2 pulse-icon"></i>
                                    Ajouter un Projet Print
                                </span>
                                <div class="wave-effect"></div>
                                <div class="wave-effect wave-2"></div>
                                <div class="wave-effect wave-3"></div>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <style>
        .add-tp-container {
            display: inline-block;
        }

        .btn-add-tp {
            border: none;
            color: white;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
            border-radius: 50px;
            transition: all 0.3s ease;
            animation: gradientShift 3s ease-in-out infinite, pulse 2s infinite;
        }

        .btn-digital {
            background: linear-gradient(45deg, var(--primary-color), var(--secondary-color));
            background-size: 300% 300%;
            box-shadow: 0 8px 25px rgba(0, 51, 102, 0.3);
        }

        .btn-print {
            background: linear-gradient(45deg, var(--accent-color), #FF9900);
            background-size: 300% 300%;
            box-shadow: 0 8px 25px rgba(255, 102, 51, 0.3);
        }

        .btn-add-tp:hover {
            transform: translateY(-3px);
            color: white;
        }

        .btn-digital:hover {
            box-shadow: 0 12px 35px rgba(0, 51, 102, 0.4);
        }

        .btn-print:hover {
            box-shadow: 0 12px 35px rgba(255, 102, 51, 0.4);
        }

        .btn-add-tp:active {
            transform: translateY(-1px);
        }

        .btn-content {
            z-index: 2;
        }

        .pulse-icon {
            animation: iconPulse 1.5s ease-in-out infinite;
        }

        .wave-effect {
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            transform: translate(-50%, -50%);
            animation: waveRipple 2s infinite;
        }

        .wave-2 {
            animation-delay: 0.7s;
        }

        .wave-3 {
            animation-delay: 1.4s;
        }

        @keyframes gradientShift {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        @keyframes pulse {
            0% { box-shadow: 0 8px 25px rgba(0, 51, 102, 0.3); }
            50% { box-shadow: 0 8px 25px rgba(51, 153, 255, 0.4), 0 0 20px rgba(255, 102, 51, 0.3); }
            100% { box-shadow: 0 8px 25px rgba(0, 51, 102, 0.3); }
        }

        @keyframes iconPulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.2); }
        }

        @keyframes waveRipple {
            0% {
                width: 0;
                height: 0;
                opacity: 1;
            }
            50% {
                width: 100px;
                height: 100px;
                opacity: 0.5;
            }
            100% {
                width: 200px;
                height: 200px;
                opacity: 0;
            }
        }

        /* Effet de clignotement subtil */
        @keyframes blink {
            0%, 50% { opacity: 1; }
            25% { opacity: 0.8; }
            75% { opacity: 0.9; }
        }

        .btn-add-tp::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            transition: left 0.5s;
        }

        .btn-add-tp:hover::before {
            left: 100%;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .btn-add-tp {
                padding: 15px 30px;
                font-size: 16px;
            }
        }
        </style>


    </div>
</div>
@endsection
