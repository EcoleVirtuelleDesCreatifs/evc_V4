@extends('layouts.ki-admin')

@section('title', 'Bibliothèque - EVC 2024')
@section('page-title', 'Bibliothèque')

@section('content')
<div class="container-fluid">
    
    <!-- En-tête avec statistiques globales -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="card" style="background: linear-gradient(135deg, #1e3a8a 0%, #3b82f6 100%); border: none;">
                <div class="card-body text-white text-center py-4">
                    <h2 class="mb-2">
                        <i class="fas fa-books me-2"></i>
                        Bibliothèque - {{ ucfirst(str_replace('-', ' ', $currentModule ?? 'Module')) }}
                    </h2>
                    <p class="mb-0 opacity-75">Accédez à toutes vos ressources pédagogiques</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Statistiques par catégorie -->
    <div class="row mb-4">
        @foreach($categories as $index => $category)
            @php
                $key = strtolower(str_replace(' ', '_', $category->name));
                $count = $stats[$key] ?? 0;
                $colors = ['#667eea', '#f093fb', '#4facfe', '#43e97b', '#fa709a'];
                $icons = ['fas fa-box-open', 'fas fa-book', 'fas fa-file-pdf', 'fas fa-folder-open', 'fas fa-archive'];
                $color = $colors[$index % count($colors)];
                $icon = $icons[$index % count($icons)];
            @endphp
            <div class="col-lg-3 col-md-4 col-sm-6 mb-3">
                <div class="card h-100 border-0 shadow-sm" style="transition: all 0.3s ease;">
                    <div class="card-body text-center">
                        <div class="mb-3" style="background: {{ $color }}20; width: 70px; height: 70px; border-radius: 50%; margin: 0 auto; display: flex; align-items: center; justify-content: center;">
                            <i class="{{ $icon }} fa-2x" style="color: {{ $color }};"></i>
                        </div>
                        <h3 class="mb-1" style="color: {{ $color }}; font-weight: bold;">{{ $count }}</h3>
                        <small class="text-muted">{{ $category->name }}</small>
                        <a href="#category-{{ $index }}" class="btn btn-sm w-100 mt-3" style="background: {{ $color }}15; color: {{ $color }}; border: 1px solid {{ $color }}30;">
                            <i class="fas fa-arrow-down me-1"></i>
                            Voir les documents
                        </a>
                    </div>
                </div>
            </div>
        @endforeach
        
        <!-- Carte Total -->
        <div class="col-lg-3 col-md-4 col-sm-6 mb-3">
            <div class="card h-100 border-0 shadow-sm" style="background: linear-gradient(135deg, #1e3a8a 0%, #3b82f6 100%);">
                <div class="card-body text-center text-white">
                    <div class="mb-3" style="background: #ffffff30; width: 70px; height: 70px; border-radius: 50%; margin: 0 auto; display: flex; align-items: center; justify-content: center;">
                        <i class="fas fa-layer-group fa-2x"></i>
                    </div>
                    <h3 class="mb-1 fw-bold">{{ $stats['total'] ?? 0 }}</h3>
                    <small class="opacity-75">Total Documents</small>
                </div>
            </div>
        </div>
    </div>

    <!-- Sections dynamiques par catégorie de bibliothèque -->
    @foreach($categories as $index => $category)
        @if(isset($documentsParCategorie[$category->name]) && count($documentsParCategorie[$category->name]) > 0)
        <div class="row mb-4" id="category-{{ $index }}">
            <div class="col-12">
                <div class="card">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h5 class="mb-0">
                            @php
                                $icons = ['fas fa-box-open', 'fas fa-book', 'fas fa-file-alt'];
                                $colors = ['var(--primary-color)', 'var(--secondary-color)', 'var(--success-color)'];
                                $icon = $icons[$index % count($icons)];
                                $color = $colors[$index % count($colors)];
                            @endphp
                            <i class="{{ $icon }} me-2" style="color: {{ $color }};"></i>
                            {{ $category->name }}
                        </h5>
                        @php
                            $key = strtolower(str_replace(' ', '_', $category->name));
                        @endphp
                        <span class="badge" style="background-color: {{ $color }}; color: white;">{{ $stats[$key] ?? 0 }} document(s)</span>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            @foreach($documentsParCategorie[$category->name] as $doc)
                            <div class="col-lg-3 col-md-4 col-sm-6 mb-4">
                                <div class="card h-100 shadow-sm">
                                    <div class="position-relative">
                                        <img src="{{ $doc['image'] ?? 'https://via.placeholder.com/200x280/667eea/ffffff?text=Document' }}" 
                                             class="card-img-top" 
                                             alt="{{ $doc['titre'] }}"
                                             style="height: 280px; object-fit: cover;">
                                        <div class="position-absolute top-0 end-0 m-2">
                                            <span class="badge bg-success">{{ $doc['type'] }}</span>
                                        </div>
                                    </div>
                                    <div class="card-body d-flex flex-column">
                                        <h6 class="card-title">{{ $doc['titre'] }}</h6>
                                        <p class="card-text text-muted small flex-grow-1">{{ $doc['description'] }}</p>
                                        <div class="mb-2">
                                            <small class="text-muted">
                                                <i class="fas fa-file-pdf me-1"></i>{{ $doc['format'] }} • {{ $doc['taille'] }}
                                            </small><br>
                                            <small class="text-muted">
                                                <i class="fas fa-download me-1"></i>{{ $doc['telechargements'] }} téléchargements
                                            </small>
                                        </div>
                                        <div class="d-flex gap-2">
                                            <a href="{{ $doc['lien'] }}" target="_blank" class="btn btn-primary btn-sm flex-fill" title="Ouvrir le PDF dans un nouvel onglet">
                                                <i class="fas fa-book-open me-1"></i> Lire
                                            </a>
                                            <a href="{{ route('design-graphique.documents.download', $doc['id']) }}" class="btn btn-outline-secondary btn-sm" title="Télécharger le PDF">
                                                <i class="fas fa-download"></i>
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            @endforeach
                        </div>
                    </div>
                </div>
            </div>
        </div>
        @endif
    @endforeach

    <!-- Message si aucun document -->
    @if(empty($documents) || count($documents) == 0)
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body text-center py-5">
                    <i class="fas fa-folder-open fa-4x text-muted mb-3"></i>
                    <h4 class="text-muted">Aucun document disponible</h4>
                    <p class="text-muted">Aucun document n'est actuellement disponible pour ce module.</p>
                </div>
            </div>
        </div>
    </div>
    @endif

</div>

@section('scripts')
<script>
// Scroll fluide vers les sections
document.querySelectorAll('a[href^="#category-"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const targetId = this.getAttribute('href');
        const targetElement = document.querySelector(targetId);
        
        if (targetElement) {
            const offset = 80;
            const elementPosition = targetElement.getBoundingClientRect().top;
            const offsetPosition = elementPosition + window.pageYOffset - offset;
            
            window.scrollTo({
                top: offsetPosition,
                behavior: 'smooth'
            });
        }
    });
});
</script>
@endsection
@endsection
