{{-- Page d'erreur 500 temporairement désactivée pour debug --}}
{{-- @extends('layouts.ki-admin')

@section('title', 'Erreur Serveur - 500')

@section('content') --}}
<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-lg-8 col-md-10">
            <div class="card shadow-lg border-0" style="margin-top: 50px;">
                <div class="card-body text-center p-5">
                    
                    {{-- Logo et branding --}}
                    <div class="mb-4">
                        <div class="error-icon mb-3">
                            <i class="fas fa-tools" style="font-size: 80px; color: #dc3545; opacity: 0.8;"></i>
                        </div>
                        <h1 class="display-1 fw-bold text-danger mb-0">500</h1>
                        <h2 class="h3 text-muted mb-4">Erreur Serveur</h2>
                    </div>

                    {{-- Message principal --}}
                    <div class="mb-4">
                        <h3 class="h4 text-dark mb-3">
                            <i class="fas fa-exclamation-triangle text-warning me-2"></i>
                            Une erreur technique s'est produite
                        </h3>
                        <p class="lead text-muted mb-4">
                            Nos serveurs rencontrent actuellement un problème technique temporaire.
                        </p>
                        <p class="text-muted">
                            Notre équipe technique a été automatiquement notifiée et travaille à résoudre ce problème rapidement.
                        </p>
                    </div>

                    {{-- Actions suggérées --}}
                    <div class="mb-4">
                        <a href="{{ route('dashboard.design-graphique') }}" class="btn btn-primary me-3">
                            <i class="fas fa-home me-2"></i>Retour au Dashboard
                        </a>
                        <button onclick="window.location.reload()" class="btn btn-outline-secondary">
                            <i class="fas fa-redo me-2"></i>Réessayer
                        </button>
                    </div>

                    {{-- Contact support --}}
                    <div class="mt-5 pt-4 border-top">
                        <h6 class="text-muted mb-3">Le problème persiste ?</h6>
                        <div class="d-flex justify-content-center gap-3">
                            <a href="mailto:info@ecolevirtuelledescreatifs.com" class="btn btn-outline-secondary btn-sm">
                                <i class="fas fa-envelope me-1"></i>Contacter le Support
                            </a>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>
</div>
@endsection
